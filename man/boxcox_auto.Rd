% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treat.R
\name{boxcox_auto}
\alias{boxcox_auto}
\title{Box-Cox transformation with automatic lambda selection}
\usage{
boxcox_auto(
  x,
  na.rm = FALSE,
  lambda_range = c(-3, 3),
  n_lambda = 61,
  makepos = TRUE
)
}
\arguments{
\item{x}{A numeric vector.}

\item{na.rm}{Logical: if \code{TRUE}, \code{NA}s are removed before estimating the lambda parameter. Default \code{FALSE}.}

\item{lambda_range}{Numeric vector of length 2 giving the lower and upper bounds for the search. Default \code{c(-3, 3)}.}

\item{n_lambda}{Integer giving the number of lambda values to evaluate between the bounds. Default \code{61}.}

\item{makepos}{Logical: passed to \code{\link[=boxcox]{boxcox()}} to ensure the data are positive prior to transformation. Default \code{TRUE}.}
}
\value{
A list with the transformed vector (\code{x}), a character vector describing the treatment applied
(\code{treated}), and diagnostic entries for the selected \code{lambda}, and skewness before and after transformation.
}
\description{
Selects the Box-Cox parameter that minimises the absolute skewness of the transformed data over
a supplied search range. The transform is then applied to the full vector, with diagnostics on the
pre- and post-treatment skewness returned for logging.
}
\examples{
set.seed(123)
x <- rexp(20, rate = 2)
boxcox_auto(x)
}
