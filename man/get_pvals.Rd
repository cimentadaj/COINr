% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlations.R, R/unbalanced_coin.R
\name{get_pvals}
\alias{get_pvals}
\alias{get_pvals.default}
\alias{get_pvals.coin}
\alias{get_pvals.unbalanced_coin}
\title{P-values for correlations}
\usage{
get_pvals(x, ...)

\method{get_pvals}{default}(x, ...)

\method{get_pvals}{coin}(
  x,
  dset,
  iCodes = NULL,
  Level = NULL,
  uCodes = NULL,
  use_group = NULL,
  also_get = "none",
  ...
)

\method{get_pvals}{unbalanced_coin}(
  x,
  dset,
  iCodes = NULL,
  Level = NULL,
  uCodes = NULL,
  use_group = NULL,
  also_get = "none",
  ...
)
}
\arguments{
\item{x}{Object to analyse. For the default method this should be a numeric data frame or matrix.}

\item{...}{Additional arguments passed to \code{\link[stats:cor.test]{stats::cor.test()}}, e.g. \code{conf.level = 0.95}.}

\item{dset}{The name of the data set to apply the function to, which should be accessible in \code{.$Data}.}

\item{iCodes}{Optional indicator codes to retrieve. If \code{NULL} (default), returns all iCodes found in
the selected data set. Can also refer to indicator groups.}

\item{Level}{Optionally, the level in the hierarchy to extract data from.}

\item{uCodes}{Optional unit codes to filter rows of the resulting data set. Can also be used in
conjunction with groups.}

\item{use_group}{Optional grouping to filter rows of the data set. Specified as \code{list(Group_Var = Group)}.}

\item{also_get}{Character vector specifying any additional columns to attach to the data set that are
not indicators or aggregates. Set \code{also_get = "none"} to return only numeric columns.}
}
\value{
Matrix of p-values
}
\description{
This is a stripped down version of the "cor.mtest()" function from the "corrplot" package. It uses
the \code{\link[stats:cor.test]{stats::cor.test()}} function to calculate pairwise p-values. Unlike the corrplot version, this
only calculates p-values, and not confidence intervals. Credit to corrplot for this code, replicated
here to avoid depending on their package for a single function.
}
\details{
The generic can operate on a numeric matrix/data frame directly, or on the indicator data stored in a
\code{\link[=new_coin]{coin}} object. See the method documentation for details on additional selection arguments. When
supplied with an \code{\link[=new_unbalanced_coin]{unbalanced_coin}}, helper nodes inserted during construction are removed from both
the delegated data and the returned matrix so that only genuine indicators and aggregates are
represented.
}
\examples{
# a matrix of random numbers, 3 cols
x <- matrix(runif(30), 10, 3)

# get correlations between cols
cor(x)

# get p values of correlations between cols
get_pvals(x)

}
